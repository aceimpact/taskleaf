<% provide(:title, '新規登録') %>

<h1>タスク新規登録</h1>

<%= bootstrap_form_for(@task) do |form| %>
    <%= form.text_field :name, label: "名称" %>


    <%= form.text_area :description, label: "詳しい説明" %>

    <%= form.submit "登録する", class: "btn btn-primary" %>

  <% end %>
